---
description: Toggle typewriter mode on or off for this repo
argument-hint: on|off
---

Overwrite the file `.claude/typewriter-mode` so its entire contents is exactly `$ARGUMENTS` (must be `on` or `off`, nothing else). Then reply with one sentence confirming the new state.
